security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        adherent_provider:
            entity:
                class: App\Entity\Adherent
                property: email

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: adherent_provider
            entry_point: App\Security\LoginEntryPoint
            custom_authenticators:
                - App\Security\DummyAuthenticator
            logout:
                path: app_logout
                target: app_home

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    access_control:
        # Page de vérification d'email uniquement en accès public
        - { path: ^/verify-email, roles: PUBLIC_ACCESS }
        # Page de login également publique
        - { path: ^/login, roles: PUBLIC_ACCESS }
        # Backoffice réservé aux administrateurs
        - { path: ^/admin, roles: ROLE_ADMIN }
        # Tout le reste du site nécessite au minimum d'être connecté
        - { path: ^/, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
